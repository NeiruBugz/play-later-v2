# E2E Test Environment Configuration
# This file is used by E2E tests to connect to a SEPARATE test database
# to avoid affecting development data.

# Test Database Configuration - Uses a SEPARATE database from dev
POSTGRES_PRISMA_URL=postgresql://postgres:postgres@localhost:6432/savepoint-db-test
POSTGRES_URL_NON_POOLING=postgresql://postgres:postgres@localhost:6432/savepoint-db-test
POSTGRES_URL=postgresql://postgres:postgres@localhost:6432/savepoint-db-test
POSTGRES_USER=postgres
POSTGRES_HOST=localhost
POSTGRES_PASSWORD=postgres
POSTGRES_DATABASE=savepoint-db-test
POSTGRES_URL_NO_SSL=postgresql://postgres:postgres@localhost:6432/savepoint-db-test

# Test Auth Configuration
AUTH_SECRET=test-secret-for-e2e-testing-only
AUTH_URL=http://localhost:6060
# Better Auth secrets — required for BA's session signing and base URL
BETTER_AUTH_SECRET=test-better-auth-secret-for-e2e-testing-only
BETTER_AUTH_URL=http://localhost:6060
# Must be "true" so BA's email+password plugin is enabled during E2E runs
AUTH_ENABLE_CREDENTIALS=true
