# pnpm Configuration

# Hoist all dependencies to node_modules/.pnpm for better Next.js compatibility
shamefully-hoist=true

# Auto-install peer dependencies to avoid manual installation
auto-install-peers=true

# Don't fail on peer dependency conflicts (common in React ecosystem)
strict-peer-dependencies=false
