# Code Architecture — Audit Results

**Date:** 2026-05-12
**Score:** 89% — Grade **B**

## Results

| #   | Check                                                  | Severity | Status | Evidence |
| --- | ------------------------------------------------------ | -------- | ------ | -------- |
| 1   | ARCH-01 Declared or recognizable architectural pattern | high     | PASS   | Feature-Sliced Design explicitly declared and applied in both apps: `savepoint-app/{app,features,widgets,shared,data-access-layer}` (DAL adds Clean-Arch layers `handlers/services/repository/domain` — see `data-access-layer/CLAUDE.md`); `savepoint-tanstack/src/{app,routes,widgets,features,entities,shared}` matches canonical FSD layout; `infra/` follows Terraform modules+envs pattern (`infra/modules/{cognito,s3}` + `envs/{dev,prod}`) |
| 2   | ARCH-02 Module boundaries are respected                | high     | WARN   | savepoint-app: 4 features import from `@/widgets/game-card`, violating the explicit rule "Features must NOT import from widgets/" in `savepoint-app/features/CLAUDE.md`: `features/browse-related-games/ui/related-games.tsx`, `features/game-search/ui/game-card.tsx`, `features/journal/ui/journal-entry-card.tsx`, `features/journal/ui/journal-timeline.tsx`. Otherwise direction is consistent: 0 shared→features, 0 shared→widgets, 0 entities→features violations; savepoint-tanstack clean (0 features→widgets, 0 shared→features/widgets) |
| 3   | ARCH-03 Single Responsibility Principle in modules     | medium   | PASS   | All top-level modules carry descriptive names with focused contents: `features/*` named by capability (library, journal, steam-import, command-palette…), `data-access-layer/{handlers,services,repository,domain}`, `widgets/*` named by UI region. No `utils/`/`helpers/`/`common/`/`misc/` catch-alls. Largest single dir `features/library/ui` has 29 files but all relate to the library-card surface (cta, menu, metadata, list-row variants). `shared/lib` segments by concern (igdb, platform, prisma, steam, storage, server-action, errors) rather than dumping ground |
| 4   | ARCH-04 Separation of concerns across layers           | high     | PASS   | Sampled features (`library`, `journal`, `steam-import`) each split into `ui/`, `hooks/`, `server-actions/`, `lib/`, `schemas.ts`, `types.ts`; data access centralized in `data-access-layer/{handlers→services→repository}` (Clean-Arch flow documented in `data-access-layer/CLAUDE.md`); savepoint-tanstack mirrors split via `features/<name>/{ui,api}` with server functions isolated from components. No SQL/Prisma calls observed inline in UI files in samples |
| 5   | ARCH-05 Consistent file and directory naming           | medium   | PASS   | Uniform kebab-case across both apps for files and directories (`library-card-cta.tsx`, `update-library-status.ts`); 0 PascalCase source files outside generated `shared/lib/prisma/` (11 generated `*.ts` excluded). Tests colocated as `*.test.tsx`/`*.unit.test.ts`/`*.integration.test.ts` next to source; minor `__tests__/` directories present in 3 spots (`data-access-layer/handlers/steam-import`, `data-access-layer/services/steam`, `features/steam-import/server-actions`) — small deviation, otherwise consistent |
| 6   | ARCH-06 Reasonable file sizes                          | medium   | PASS   | savepoint-app (excluding generated `shared/lib/prisma/*`): 577 source files, 3 over 500 lines (0.5%), 0 over 1000. Largest: `data-access-layer/repository/library/library-repository.ts` (841), `shared/components/ui/sidebar.tsx` (770, shadcn-vendored), `features/steam-import/ui/imported-games-list.tsx` (683). savepoint-tanstack: 255 source files (excl. `routeTree.gen.ts`), 0 over 500. Note: 11 generated Prisma model files in `savepoint-app/shared/lib/prisma/models/` are 1450–3299 lines but explicitly marked "code generated by Prisma. Do not edit directly." — excluded per check definition |

## Architecture Summary

- **Pattern (both JS apps):** Feature-Sliced Design (FSD) — `app → routes/pages → widgets → features → entities → shared` direction enforced via per-layer `CLAUDE.md` rules and cross-feature allowlist tables.
- **Pattern (savepoint-app DAL):** Layered Clean Architecture inside FSD `shared`-adjacent layer: `handlers` (transport/Result-shaped) → `services` (business) → `repository` (Prisma) → `domain` (types/mappers). Documented in `savepoint-app/data-access-layer/CLAUDE.md`.
- **Pattern (infra):** Terraform module-per-resource (`infra/modules/{cognito,s3}`) consumed by env roots (`infra/envs/{dev,prod}`).
- **Per-app health:**
  - `savepoint-app/`: FSD with 4 documented boundary violations (features→widgets/game-card); 3 files (0.5%) over 500 LOC; otherwise compliant.
  - `savepoint-tanstack/`: FSD with 0 detected layer violations; 0 files over 500 LOC; cleaner due to fresher codebase under spec 021 migration.
- **Generated code excluded:** `savepoint-app/shared/lib/prisma/` (11 Prisma-generated `.ts`, `.server.ts` files, 1450–3299 LOC, `@ts-nocheck`).
- **Anti-patterns observed:** None of god-modules, mixed-concern files, flat dumps, or naming chaos. Only friction is the 4 features→widgets imports (boundary leakage), which downgrades ARCH-02 to WARN despite explicit project rule prohibiting it.

### Recommended fixes (for downstream recommendations file)

- P2 (ARCH-02 high WARN): Either (a) lift `widgets/game-card` exports needed by features into a shared cross-feature module (e.g., `entities/game/ui/`), or (b) refactor the 4 offending features to receive game-card pieces via composition from a widget/page rather than importing upward, or (c) update the allowlist in `features/CLAUDE.md` to legitimize these imports and document the exception.
